services:
  redis:
    image: redis
    ports:
      - 6379:6379
    volumes:
      - ./redis_data:/data
    restart: always
    networks:
      - redis_net

networks:
  redis_net: 
    driver: bridge
