name: ETL Pipeline

on:
  workflow_dispatch:
  #push:
  #  branches: [ "main" ]
  #pull_request:
  #  branches: [ "main" ]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test_job:
    env:
      DATA_SOURCE_IMG: "ecommerce:latest"
      ETL_IMG: "etl:latest"
      NETWORK_NAME: analytics_network
    
    runs-on: ubuntu-22.04

    steps:
    
    - uses: actions/checkout@v4

    - name: Create docker network
      shell: bash
      run: docker network create --attachable --driver bridge $NETWORK_NAME
    
    - name: Verify network
      shell: bash
      run: docker network ls
    
    - name: Create MySQL .env
      shell: bash
      working-directory: ./infra/mysql
      run: |
        echo "MYSQL_ROOT_PASSWORD=${{ secrets.MYSQL_ROOT_PASSWORD }}" >> .env
        echo "MYSQL_USER=${{ secrets.MYSQL_USER }}" >> .env
        echo "MYSQL_PASSWORD=${{ secrets.MYSQL_PASSWORD }}" >> .env
        echo "MYSQL_DATABASE=${{ secrets.MYSQL_DATABASE }}" >> .env
    
    - name: Setup MySQL
      shell: bash
      working-directory: ./infra/mysql
      run: docker compose up --build -d
    
    - name: Verify MySQL
      shell: bash
      run: docker ps # && sleep 40 && docker ps
    
#    - name: Test MySQL CLI in container
#      shell: bash
#      run: |
#        MYSQL_USR=${{ secrets.MYSQL_USER }}
#        docker exec -i -e MYSQL_PWD=${{ secrets.MYSQL_PASSWORD }} mysql_server mysql -h 127.0.0.1 -u$MYSQL_USR -e "SELECT 1;"
    - name: Create .env file for Clickhouse
      working-directory: ./infra/clickhouse
      shell: bash
      run: |
        echo "CLICKHOUSE_USER=${{ secrets.CLICKHOUSE_USER }}" >> .env
        echo "CLICKHOUSE_PASSWORD=${{ secrets.CLICKHOUSE_PASSWORD }}" >> .env

    - name: Verify .env
      working-directory: ./infra/clickhouse
      shell: bash
      run: ls -l .env

    - name: Clickhouse docker compose
      shell: bash
      working-directory: ./infra/clickhouse
      run: |
        docker compose up --build -d
    
    - name: Verify Clickhouse
      shell: bash
      run: |
        docker ps
    
    - name: Create .env
      working-directory: ./services/static-ecommerce
      shell: bash
      run: |
        echo "MYSQL_USER=${{ secrets.MYSQL_USER }}" >> .env
        echo "MYSQL_PASSWORD=${{ secrets.MYSQL_PASSWORD }}" >> .env
        echo "MYSQL_DATABASE=${{ secrets.MYSQL_DATABASE }}" >> .env
        echo "MYSQL_HOST=mysql_server" >> .env
        echo "MYSQL_PORT=3306" >> .env
    
    - name: Bulid ecommerce
      shell: bash
      working-directory: ./services/static-ecommerce
      run: |
        docker build -t $DATA_SOURCE_IMG .
    
    - name: Run ecommerce
      shell: bash
      working-directory: ./services/static-ecommerce
      run: |
        docker run \
          --network $NETWORK_NAME \
          --env-file .env \
          $DATA_SOURCE_IMG
    
    - name: Create .env for ETL
      working-directory: ./services/etl
      shell: bash
      run: |
        echo "MYSQL_USER=${{ secrets.MYSQL_USER }}" >> .env
        echo "MYSQL_PASSWORD=${{ secrets.MYSQL_PASSWORD }}" >> .env
        echo "MYSQL_DATABASE=${{ secrets.MYSQL_DATABASE }}" >> .env
        echo "MYSQL_HOST=mysql_server" >> .env
        echo "MYSQL_PORT=3306" >> .env
        echo "CLICKHOUSE_USER=${{ secrets.CLICKHOUSE_USER }}" >> .env
        echo "CLICKHOUSE_PASSWORD=${{ secrets.CLICKHOUSE_PASSWORD }}" >> .env
        echo "CLICKHOUSE_HOST=clickhouse_server" >> .env
        echo "CLICKHOUSE_PORT=8123" >> .env
    
    - name: Bulid ETL
      shell: bash
      working-directory: ./services/etl
      run: |
        docker build -t $ETL_IMG .
    
    - name: Run ETL
      shell: bash
      working-directory: ./services/etl
      run: |
        docker run \
          --network $NETWORK_NAME \
          --env-file .env \
          $ETL_IMG --user 100 --product 50 --order 1000